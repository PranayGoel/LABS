#include<stdio.h>
#include stdioh>

int helloword = "helloword";
int forelse if ;;
void main()
{
	FILE *fopen(), *fp;
	int c ;
	abc = 89;
	fp = fopen( “prog.c”, “r” ); //Comment
	printf("\n#def");
	printf("return for if else")
		
		c = getc( fp ) ;
	while ( c != EOF ) {
		putchar( c );
		c = getc ( fp );
	}
	/*multiline
	comment */
	// singleline comment
	fclose(fp );
}